# 🎨 Stable Diffusion WebUI + GitHub Pages + Cloudflare Tunnel

Веб-портал для управління Stable Diffusion через GitHub Pages з туннельованім доступом до локального сервера.

## ✨ Функціональність

- 🖼️ **Метадані зображень**: Перегляд та редагування EXIF/PNG метаданих
- 🎯 **Text-to-Image**: Генерування зображень з тексту через API
- 🖌️ **Image-to-Image**: Модифікація існуючих зображень
- 🖍️ **Inpainting**: Редагування конкретних частин зображень
- 📦 **Dataset Creator**: Створення датасетів з тегами для LoRA тренування
- 🔧 **Cloudflare Tunnel**: Безпечне туннелювання до локального сервера
- 💾 **Локальне збереження**: Налаштування зберігаються у браузері

## 🚀 Швидкий старт

### 1️⃣ Встановіть залежності

**cloudflared** (туннель)
```bash
# Windows
choco install cloudflared

# macOS
brew install cloudflare/cloudflare/cloudflared

# Linux
wget https://github.com/cloudflare/wrangler/releases/download/wrangler-v3.0.0/cloudflared-linux-amd64
chmod +x cloudflared-linux-amd64
sudo mv cloudflared-linux-amd64 /usr/local/bin/cloudflared
```

### 2️⃣ Запустіть Stable Diffusion з API

**На локальній машині:**
```bash
python launch.py --api --cors-allow-origins=* --listen 127.0.0.1 --port 7860
```

**На Google Colab:**
```bash
!python launch.py --api --cors-allow-origins=* --listen 127.0.0.1
```

### 3️⃣ Запустіть Cloudflare Tunnel

**Windows (PowerShell):**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
.\start-tunnel.ps1 -Port 7860
```

**Linux/macOS:**
```bash
chmod +x start-tunnel.sh
./start-tunnel.sh start 7860
```

Скопіюйте URL типу: `https://xxxx-xxxx.trycloudflare.com`

### 4️⃣ Відкрийте GitHub Pages сайт

```
https://YOUR_USERNAME.github.io/Stable_Diffusion/poyo_test_sd/
```

### 5️⃣ Налаштуйте туннель

1. Перейдіть на вкладку **#settings** (Stable Diffusion → #settings)
2. Введіть URL туннелю в поле "Cloudflared Tunnel URL"
3. Натисніть "Test Connection"
4. Готово! URL автоматично збережено

## 📁 Структура файлів

```
├── index.html                    # Головна сторінка
├── script.js                     # Основна логіка інтерфейсу
├── style.css                     # Стилі (Discord-подібний дизайн)
├── cloudflare-tunnel.js          # Управління туннелем
├── sd-api-client.js              # Клієнт Stable Diffusion API
├── stable-diffusion-api.js       # Старший API (якщо використовується)
│
├── start-tunnel.ps1              # Запуск туннелю (Windows)
├── start-tunnel.sh               # Запуск туннелю (Linux/macOS)
│
├── CLOUDFLARE_SETUP.md           # Інструкція: Cloudflare Tunnel
├── GITHUB_DEPLOY.md              # Інструкція: GitHub Pages
├── README.md                     # Цей файл
│
├── img/
│   ├── server.png
│   ├── discord.png
│   └── civitai.png
│
└── server/
    └── Google_Colab_Backend.ipynb # Google Colab notebook
```

## 🔧 Конфігурація

### Cloudflare Tunnel Manager (`cloudflare-tunnel.js`)

Автоматично:
- Зберігає URL туннелю в `localStorage`
- Тестує з'єднання
- Показує статус (зелена/жовта/червона крапка)

### Stable Diffusion API Client (`sd-api-client.js`)

Підтримує:
- `getModels()` - Список моделей
- `getSamplers()` - Список сампліршів
- `generateImage()` - txt2img генерування
- `generateImageFromImage()` - img2img генерування
- `getProgress()` - Прогрес генерування
- `getLoRAs()` - Список LoRA моделей
- `getVAEs()` - Список VAE моделей

## 🔐 Безпека

✅ **HTTPS**: GitHub Pages автоматично HTTPS
✅ **Зашифрований туннель**: Cloudflare Tunnel використовує TLS
✅ **Без публічного доступу**: Туннель закритий з боку мережи
✅ **Динамічні URL**: Кожна сесія має новий URL

⚠️ **Помните:**
- Туннель видимий всім, хто має URL
- Не публікуйте URL в соціальних мережах
- Використовуйте CORS обережно

## 📚 Документація

- [CLOUDFLARE_SETUP.md](./CLOUDFLARE_SETUP.md) - Детальна інструкція для туннелю
- [GITHUB_DEPLOY.md](./GITHUB_DEPLOY.md) - Розгортання на GitHub Pages
- [Stable Diffusion API Docs](https://github.com/AUTOMATIC1111/stable-diffusion-webui/wiki/API)
- [Cloudflare Tunnel Docs](https://developers.cloudflare.com/cloudflare-one/)

## 🎯 Використання

### Перегляд метаданих зображень

1. Перейдіть на вкладку **#viewing** (Metadata → #viewing)
2. Перетягніть PNG/JPG зображення на область
3. Переглядайте параметри, промпти та LoRA

### Редагування метаданих

1. Перейдіть на вкладку **#editing** (Metadata → #editing)
2. Завантажте зображення
3. Редагуйте промпти та параметри
4. Натисніть "Save & Download Image"

### Генерування зображень

1. Перейдіть на вкладку **#txt2img** (Stable Diffusion → #txt2img)
2. Введіть промпт та параметри
3. Натисніть "Generate"
4. Чекайте результату

### Створення датасету для LoRA

1. Перейдіть на вкладку **#dataset** (Metadata → #dataset)
2. Завантажте зображення (ZIP або окремі файли)
3. Додайте теги кожному зображенню
4. Завантажте готовий датасет

## 🆘 Вирішення проблем

### Туннель не запускається
```bash
# Перевірте cloudflared
cloudflared --version

# Перевірте, чи SD запущена
curl http://localhost:7860
```

### API не дозволяє запити
```bash
# Переконайтеся в параметрах запуску
python launch.py --api --cors-allow-origins=* --listen 127.0.0.1
```

### Z'єднання з GitHub Pages не працює
1. Перевірте URL в браузері (має бути HTTPS)
2. Перевірте консоль F12 для помилок
3. Очистіть кеш браузера (Ctrl+Shift+Delete)

### Туннель часто змінює URL
Це нормально! Cloudflare генерує нові URL. Просто оновіть в налаштуваннях.

## 💡 Поради

1. **Зберіжіть URL**: Введіть його у налаштування - збережеться в localStorage
2. **Перевіряйте з'єднання**: Натисніть "Test Connection" в налаштуваннях
3. **Используйте CORS**: `--cors-allow-origins=*` необхідний для веб-запитів
4. **Налладжування**: Див. браузерну консоль (F12) для помилок

## 📊 Архітектура

```
┌─────────────────────────────┐
│  GitHub Pages (HTTPS)       │
│  - cloudflare-tunnel.js     │
│  - sd-api-client.js         │
│  - UI & Controls            │
└──────────────┬──────────────┘
               │ HTTP запити
               │
┌──────────────▼──────────────┐
│ Cloudflare Tunnel (TLS)     │
│ https://xxxx.trycloudflare  │
└──────────────┬──────────────┘
               │ localhost
               │
┌──────────────▼──────────────┐
│ Stable Diffusion WebUI      │
│ http://127.0.0.1:7860       │
│ (локально на вашій машині)   │
└─────────────────────────────┘
```

## 📝 Примітки

- Генерування зображень займає час (30 сек - 2 хвилини)
- Туннель повинен бути завжди запущений для роботи
- URL змінюється при перезапуску туннелю
- Датасет창менший за 50 МБ на файл

## 🤝 Внесення вкладу

Знайшли баг? Пропоную покращення?
- Відкрийте Issue на GitHub
- Зробіть Pull Request з вашими змінами
- Поділіться ідеями в Discussions

## 📄 Ліцензія

MIT License - вільний до використання та модифікації

## 🙏 Дякую за використання!

Якщо це було корисним, дайте зірочку ⭐

---

**Версія**: 1.0.0  
**Останнє оновлення**: 2025-11-21  
**Статус**: ✅ Активно підтримується
